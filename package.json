{
  "name": "@expressive/window",
  "version": "1.0.0-beta.0",
  "module": "src",
  "main": "lib",
  "types": "index.d.ts",
  "files": [
    "lib/*.js",
    "index.d.ts"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --watchAll",
    "prebuild": "rm -rf lib/",
    "publish:beta": "npm version prerelease --preid=beta",
    "preversion": "jest",
    "postversion": "npm run build; npm publish",
    "postpublish": "git push; git push --tags; rm -rf lib"
  },
  "dependencies": {
    "@expressive/mvc": "^1.0.0-beta.19"
  },
  "devDependencies": {
    "@testing-library/react": "^11.0.4",
    "@types/jest": "^27.0.2",
    "@types/react": "^17.0.34",
    "jest": "^27.3.1",
    "react": "^17.0.2",
    "react-dom": "17.0.2",
    "ts-jest": "^27.0.7",
    "typescript": "^3.9.6"
  }
}
